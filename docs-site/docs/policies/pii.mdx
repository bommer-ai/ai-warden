---
title: PII Protection
description: Redact personally identifiable information before it reaches the LLM
sidebar_position: 3
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# PII Protection

**Type:** `pii` | **Priority:** 90 | **Hooks:** pre | **Default:** Enabled

Redacts personally identifiable information from the request before it reaches the LLM. The model never sees the real values — only `[REDACTED:type]` placeholders.

---

## How it works

1. Before the LLM call, PII Protection scans all message content
2. Matches are replaced with `[REDACTED:pattern_name]` tokens
3. The LLM receives sanitized input — it can reason about structure but never sees real data

```text
Input:  "Contact me at john@acme.com, SSN: 123-45-6789"
Output: "Contact me at [REDACTED:email], SSN: [REDACTED:ssn]"
→ This sanitized version is what the LLM sees
```

---

## Default patterns (zero config)

```yaml
policies:
  - name: pii-protection
    type: pii
```

| Pattern | What it matches | Example |
|---------|----------------|---------|
| `email` | Email addresses | `user@example.com` → `[REDACTED:email]` |
| `phone` | Phone numbers (US/intl) | `+1-555-0123` → `[REDACTED:phone]` |
| `ssn` | US Social Security Numbers | `123-45-6789` → `[REDACTED:ssn]` |
| `cc` | Credit card numbers | `4111-1111-1111-1111` → `[REDACTED:cc]` |
| `api_key` | API key formats (`sk-...`) | `sk-abc123...` → `[REDACTED:api_key]` |

---

## Adding custom patterns

```yaml
policies:
  - name: pii-protection
    type: pii
    patterns:
      employee_id: "\\bEMP-\\d{6}\\b"
      internal_ip: "\\b10\\.\\d+\\.\\d+\\.\\d+\\b"
      account_number: "\\bACCT-[A-Z0-9]{8}\\b"
      mrn: "\\bMRN-\\d{8}\\b"
```

:::tip Custom patterns stack with builtins
Your patterns are added alongside the defaults. Each key becomes the redaction label:
```
"Patient MRN-12345678 at 10.0.1.50"
→ "Patient [REDACTED:mrn] at [REDACTED:internal_ip]"
```
:::

---

## Disabling a built-in pattern

```yaml
policies:
  - name: pii-protection
    type: pii
    patterns:
      cc: false          # disable credit card detection
      phone: false       # disable phone number detection
```

Set to `false` to disable. Other built-in patterns remain active.

---

## Parameters reference

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `patterns` | dict | `{}` | Custom patterns (string = regex) or disabled builtins (`false`) |
| `agents` | list | `[]` | Scope to specific agents. Empty = all. |

---

## What gets scanned

- ✅ User messages (string and content-block formats)
- ✅ System prompt content
- ✅ All `text` type content blocks
- ❌ Tool results (outputs, not inputs)
- ❌ Image content blocks
- ❌ Non-content fields (model, max_tokens)

---

## Performance

- Patterns compiled once at startup
- Typical overhead: < 1ms for messages under 10KB
- Proportional to message length × number of patterns

:::tip For high-throughput batch scenarios
Disable patterns you don't need to reduce scan time.
:::

---

## Examples

<Tabs>
<TabItem value="healthcare" label="Healthcare (HIPAA)">

```yaml
- name: hipaa-pii
  type: pii
  patterns:
    mrn: "\\bMRN[-:]?\\d{6,10}\\b"
    dob: "\\b\\d{2}/\\d{2}/\\d{4}\\b"
    npi: "\\b\\d{10}\\b"
```

</TabItem>
<TabItem value="financial" label="Financial">

```yaml
- name: financial-pii
  type: pii
  patterns:
    routing_number: "\\b\\d{9}\\b"
    swift_code: "\\b[A-Z]{6}[A-Z0-9]{2}([A-Z0-9]{3})?\\b"
    iban: "\\b[A-Z]{2}\\d{2}[A-Z0-9]{4,30}\\b"
```

</TabItem>
<TabItem value="agent" label="Agent-specific">

```yaml
- name: pii-chatbot-only
  type: pii
  agents: ["chatbot", "support-bot"]
  patterns:
    employee_id: "\\bEMP-\\d{6}\\b"
```

</TabItem>
</Tabs>
